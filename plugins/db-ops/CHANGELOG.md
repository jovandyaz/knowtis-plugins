# Changelog

All notable changes to the `db-ops` plugin.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [0.1.0] - 2026-07-02

### Added

- `managing-drizzle-migrations` skill with the migrations runbook (from `docs/MIGRATIONS.md`).
- `investigating-postgres` skill with read-only contract, schema map, and 3 eval cases (happy path, mutation refusal, empty result).
